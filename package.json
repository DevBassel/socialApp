{
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/serve-static": "^4.0.1",
    "api": "^6.1.1",
    "morgan": "^1.10.0"
  }
}
