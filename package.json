{
  "devDependencies": {
    "turbo": "^1.12.5"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run build && node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
