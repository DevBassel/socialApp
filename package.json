{
  "devDependencies": {
    "turbo": "^1.12.5"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/serve-static": "^4.0.1",
    "api": "^6.1.1"
  }
}
